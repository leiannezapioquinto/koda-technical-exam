APP_NAME=Projexia
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost:8080
APP_PORT=8080
DB_DATABASE=projexia
DB_USERNAME=
DB_PASSWORD=
DB_PORT=5432
SESSION_SECURE_COOKIE=false
DB_PORT_HOST=55432
# Seeds demo login (demo@projexia.test) and sample projects on first `docker compose up`.
# Leave false for production; set true only for local/demo environments.
SEED_DEMO_DATA=false
